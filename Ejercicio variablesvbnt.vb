' Este codigo ha sido generado por el modulo psexport 20180802-w32 de PSeInt.
' Es posible que el codigo generado no sea completamente correcto. Si encuentra
' errores por favor reportelos en el foro (http://pseint.sourceforge.net).

Module EJERCICIO_1

	Sub Main()
		Dim anio2 As String
		Dim dato2 As String
		Dim dav As Integer
		Dim lco As Boolean
		Dim logi As Boolean
		Dim opl As Boolean
		Dim port As String
		Dim vilm As Integer
		Dim weezer As Double
		Dim anio(5) As String
		dav = 5
		port = "5"
		dato2 = "5"
		vilm = -4
		weezer = 3.156
		anio(1) = "2"
		anio(2) = "0"
		anio(3) = "1"
		anio(4) = "9"
		anio2 = "2019"
		opl = True
		logi = False
		lco = False
		Console.WriteLine("Estos son las variables definidas ")
		Console.WriteLine(dav)
		Console.WriteLine(vilm)
		Console.WriteLine(port)
		Console.WriteLine("Esta es una variable del tipo caracter")
		Console.WriteLine(anio(1),anio(2),anio(3),anio(4))
		Console.WriteLine(dato2)
		Console.WriteLine(anio2)
		Console.WriteLine(weezer)
		Console.WriteLine(opl)
		Console.WriteLine(logi)
		Console.WriteLine(lco)
	End Sub

End Module
